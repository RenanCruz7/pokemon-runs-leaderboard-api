services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=leaderboard_db'
      - 'POSTGRES_PASSWORD=Vieira0104%'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432'
